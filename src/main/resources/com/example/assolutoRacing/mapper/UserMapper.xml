<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.example.assolutoRacing.Mapper.UserMapper">
  <insert id="insert">
    insert into user
    (
     user_name,
     password,
     create_time,
     update_time,
     delete_flag
    )
    values(
     #{userName},
     #{password},
     NOW(),
     NOW(),
     false
    )
  </insert>
  
  <select id="selectUserCountByUserName" resultType="Integer">
   select 
    COUNT(*) 
   from user
   where 
   user_name = #{userName}
  </select>
  
  <select id="auth" resultType="Integer">
    select
     COUNT(*)
    from user
    <where>
    user_name = #{userName}
    and 
    password = #{password}
    </where>
  </select>
</mapper>